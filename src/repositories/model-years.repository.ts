import knex from '../databases/knex.database';

export default class ModelYearsRepository {
    public insertAll = async () => {
        try {

        } catch (error) {
            throw error;
        }
    };
};